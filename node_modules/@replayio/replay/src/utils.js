"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.isValidUUID = exports.getDirectory = exports.maybeLog = exports.defer = void 0;
const path_1 = __importDefault(require("path"));
function defer() {
    let resolve = () => { };
    let reject = () => { };
    const promise = new Promise((res, rej) => {
        resolve = res;
        reject = rej;
    });
    return { promise, resolve, reject };
}
exports.defer = defer;
function maybeLog(verbose, str) {
    if (verbose) {
        console.log(str);
    }
}
exports.maybeLog = maybeLog;
function getDirectory(opts) {
    const home = process.env.HOME || process.env.USERPROFILE;
    return ((opts && opts.directory) ||
        process.env.RECORD_REPLAY_DIRECTORY ||
        path_1.default.join(home, ".replay"));
}
exports.getDirectory = getDirectory;
function isValidUUID(str) {
    if (typeof str != "string" || str.length != 36) {
        return false;
    }
    for (let i = 0; i < str.length; i++) {
        if ("0123456789abcdef-".indexOf(str[i]) == -1) {
            return false;
        }
    }
    return true;
}
exports.isValidUUID = isValidUUID;
//# sourceMappingURL=utils.js.map